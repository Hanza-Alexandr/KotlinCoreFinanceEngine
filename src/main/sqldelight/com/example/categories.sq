-- ===========================
-- TABLE: CategoryDao
-- ===========================

CREATE TABLE CategoryDao (
  id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
  title TEXT NOT NULL,
  parent_category INTEGER,
  FOREIGN KEY (parent_category) REFERENCES CategoryDao(id)
    ON UPDATE NO ACTION ON DELETE NO ACTION
);