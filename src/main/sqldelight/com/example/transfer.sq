CREATE TABLE TransferDao (
    id INTEGER NOT NULL PRIMARY KEY,
    from_storage_id INTEGER NOT NULL REFERENCES StorageDao(id),
    to_storage_id INTEGER NOT NULL REFERENCES StorageDao(id),
    amount REAL NOT NULL,
    date TEXT NOT NULL,
    time TEXT NOT NULL,
    status TEXT NOT NULL
);
