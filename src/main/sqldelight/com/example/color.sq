CREATE TABLE ColorDao (
    id INTEGER NOT NULL PRIMARY KEY,
    user_id INTEGER REFERENCES UserDao(id) ON DELETE CASCADE,
    hex_code TEXT NOT NULL
);
selectUserColorsOrSystemColorsByUserId:
SELECT *
FROM ColorDao
WHERE user_id = ? OR user_id IS NULL;